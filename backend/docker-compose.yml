services:
  db:
    image: postgres:17.5
    container_name: app-db
    restart: always
    environment:
      POSTGRES_DB: rocket-local
      POSTGRES_USER: rocketman
      POSTGRES_PASSWORD: eltonjohn
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db:
    driver: local
